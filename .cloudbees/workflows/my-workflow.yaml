apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  workflow_dispatch:

jobs:
  call_test_properties_resolution:
    uses: mluzcabrera/testpropertyresolution/.cloudbees/workflows/test-properties-resolution.yaml